{
  "name": "patch-management",
  "version": "1.0.0",
  "description": "A patch management application similar to NinjaOne",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node build.js",
    "build:client": "cd client && npm install && npm run build",
    "build:server": "npm install",
    "package": "npm run build && cd build && tar -czf ../patch-management.tar.gz ."
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "mongoose": "^7.5.0",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
} 